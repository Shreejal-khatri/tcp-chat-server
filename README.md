# TCP Chat Server

A simple multi-client chat server built with Python sockets.  
The server accepts multiple clients simultaneously and prints messages sent by each client.  

This project demonstrates core networking concepts such as TCP connections, client-server communication, and handling multiple clients using threads.  